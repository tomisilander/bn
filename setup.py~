from distutils.core import setup

setup(name          = "bn",
      version       = "0.1",
      description   = "Bayesian network software",
      author        = "Tomi Silander",
      author_email  = "tomi.silander@iki.fi",
      packages      = ["bn","bn.learn","bn.model","bn.infer",
			"bn.util","bn.scripts"],
      package_data  = {"bn.learn":["cscore.so", "cdata.so"]},
      long_description = """
    Could be longer.

      """

      )
